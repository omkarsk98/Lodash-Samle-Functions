// reverse an array
// use native function for performance
// NOTE: does not reverse a sting

const _ = require("lodash");
let array = ["adjdfjk", 324, null, 0, "saj", undefined]; // [ undefined, 'saj', 0, null, 324, 'adjdfjk' ]
let res = _.reverse(array[0])
console.log(res)